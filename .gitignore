# Exports and rollbacks are machine-specific and may contain personal
# settings (paths, recent files, account identifiers). Never commit them.
macprefs-export/
*.plist

# macOS noise
.DS_Store
._*
.Spotlight-V100
.Trashes
